<h1>メモ一覧</h1>
<button type=“button” onclick="location.href='/memos/new'" class="post-btn">追加</button>
<ul>
  <% @memos.each do |memo| %>
    <li><a href=<%= "/memos/#{memo['id']}" %>><%= h(memo['title']) %></a></li>
  <% end %>
</ul>
